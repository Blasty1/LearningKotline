fun main() {
    var lista : List<Int> = listOf(2,4,5);
    var sett : Set<String> = setOf("oko","dfikdf","d");
    var test=10;
    println("Hello World $test + test = ${test + 1}  + ${lista} + ${sett}")
}